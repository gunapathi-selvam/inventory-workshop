# Hoisted linker: required for Expo/React Native (Metro) in a pnpm monorepo so
# Metro can resolve conventionally-hoisted deps (@babel/runtime, metro-runtime…).
# Backward-compatible with the Next.js web app.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
